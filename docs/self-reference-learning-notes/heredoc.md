# here-document

- Unquoted EOF: variable expansion happens inside the heredoc (use \ to escape if we want to prevent expansion).
- Quoted 'EOF': no variable expansion occurs inside the heredoc (literals are preserved automatically).